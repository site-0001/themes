{
  "name": "{{ $.Site.Title }}",
  "short_name": "{{ $.Site.Title }}",
  "description": "{{ $.Site.Params.description }}",
  "icons": [
    {
      "src": "{{ "img/touch-icon-google.png" | absURL }}",
      "sizes": "512x512"
    }
  ],
  "display": "fullscreen",
  "background_color": "{{ $.Site.Data.theme.color_bg }}",
  "theme_color": "{{ $.Site.Data.theme.color }}"
}